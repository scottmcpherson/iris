# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

When testing, use the ios simulator and peekabook to interact with and test the simulator. The ios simulator should already be running.